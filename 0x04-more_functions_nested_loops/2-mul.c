#include "main.h"
/**
 * mul - returns multiplication of two intigers
 * @a: holds the first number
 * @b: holds the second number
 * Return: the product
 */

int mul(int a, int b)
{
	return (a * b);
}
