#include <stdio.h>
#include <stdlib.h>
/**
 * main - a program that prints its name, followed by a new line.
 * @a: the count
 * @b: the vector
 * Return: 0
 */
int main(int a, int *b[])
{
	printf("%s\n", b);
	(void)a;
	return (0);
}
