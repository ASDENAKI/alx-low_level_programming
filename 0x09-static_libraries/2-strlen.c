#include "main.h"
#include <string.h>

/**
 * _strlen - gets the length of a function
 * @s: pointer to string passed to this function
 * Return: length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
